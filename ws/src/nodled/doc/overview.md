# nodl_ogmof

`nodl_ogmof` filters point clouds using an occupancy grid map and an optional
2D radius-search outlier filter.

## Node interface

The interface below is generated from the same NoDL document used to generate
and validate the node. Included documents are resolved and merged, so the tables
show the node's complete effective interface.

```{nodl-node} /nodl/node.nodl.yaml
:title: occupancy_grid_map_outlier_filter
```
